/**
 * @file app.h
 * @brief 业务应用层全局头文件
 * @details 仅包含业务层相关的头文件，隔离底层驱动，实现模块解耦
 */

#ifndef _APP_H_
#define _APP_H_

#include <stdint.h>
#include <string.h>

/* --- 业务层模块集中包含 --- */
#include "app_task.h"


/* --- 业务频率全局宏定义 --- */


#endif /* _APP_H_ */
